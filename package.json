{
  "name": "my-latex-blog-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Latex Blog",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.63.0"
  }
}
